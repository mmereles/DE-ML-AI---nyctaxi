# Copiar a .env (no se commitea) y completar con las URLs reales.

# API de cotizacion (Fase 3, ya desplegada) - sin key de OpenAI, sin costo.
VITE_QUOTE_API_URL=https://hfjeuz7wc9.execute-api.us-east-1.amazonaws.com/quote

# Agente conversacional (Fase 4.2) - Function URL del Lambda, una vez desplegado.
VITE_ASK_AGENT_URL=https://REEMPLAZAR-CON-TU-FUNCTION-URL.lambda-url.us-east-1.on.aws/
