name: terraform

on:
  pull_request:
    paths:
      - "terraform/**"
      - "lambda/**"
      - "github/workflows/terraform.yml"
  push:
    branches: [main]
    paths:
    - "terraform/**"
    - "lambda/**"
    - ".github/workflows/terraform.yml"

permissions:
  id-token: write # neccesary for OIDC
  contents: read
  pull-requests: write

env:
  AWS_REGION: us-east-1

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform

    steps:
      - name: Checkout
        uses: actions/checkout@v4
    
      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.13"
    
      - name: Install Lambda dependencies into lambda/
        working-directory: .
        run: |
          pip install \
          --platform manylinux2014_x86_64 \
          --implementation cp \
          --python-version 3.13 \
          --abi cp313 \
          --only-binary=:all: \
          --target lambda/ \
          -r lambda/requirements.txt
    
      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{env.AWS_REGION }}

      - name: Fetch Databricks token from Secrets Manager
        run: |
          TOKEN=$(aws secretsmanager get-secret-value \
            --secret-id "arn:aws:secretsmanager:us-east-1:575864492282:secret:nyctaxi/databricks-token-cW0E9g" \
            --query SecretString --output text | python3 -c "import json,sys; print(json.load(sys.stdin)['token'])")
          echo "::add-mask::$TOKEN"
          echo "DATABRICKS_TOKEN=$TOKEN" >> "$GITHUB_ENV"
    
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.10"
    
      - name: Terraform init
        run: terraform init -input=false
    
      - name: Terraform validate
        run: terraform validate
    
      - name: Terraform plan
        id: plan
        run: terraform plan -no-color -input=false -out=tfplan
    
  
